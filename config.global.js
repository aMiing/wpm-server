/**
 * 全局配置
 * 「db_config」数据库相关配置
 */

const db_config = {
    host: 'localhost',
    port: '3306',
    user: 'root',
    password: 'root',
    database: 'wpm',
};


module.exports = {
    db_config,
}